# Base URL for the istudy-cms API. Production CMS origin.
# For local development on branch `develop`, override to http://localhost:3131
# in .env.local (gitignored). Real secret values are managed via Vercel env
# (vercel env ls production) — not committed here.
NEXT_PUBLIC_CMS_URL=https://h913.aistudy.com.vn

# Branding domain rendered in footer / watermark / SEO metadata.
NEXT_PUBLIC_DOMAIN=aistudy.com.vn

# Giscus drop-in (https://giscus.app). Fill these once the GitHub repo is
# created + Discussions enabled. The Comments component renders nothing
# until all four values are present.
NEXT_PUBLIC_GISCUS_REPO=istudy/comments-discussions
NEXT_PUBLIC_GISCUS_REPO_ID=
NEXT_PUBLIC_GISCUS_CATEGORY=Bài viết
NEXT_PUBLIC_GISCUS_CATEGORY_ID=

# Cloudflare Turnstile captcha for email subscription.
# Test key: 1x00000000000000000000AA (always passes in dev).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Google AdSense client ID for banner/in-content ads.
NEXT_PUBLIC_ADSENSE_CLIENT_ID=ca-pub-0000000000000000

# AdSense slot IDs (per placement). Fill after AdSense approval; leave blank
# in dev to render the "Quảng cáo · variant" placeholder.
NEXT_PUBLIC_AD_SLOT_SIDEBAR_KHO_DE=
NEXT_PUBLIC_AD_SLOT_INLINE_BAI_VIET=
NEXT_PUBLIC_AD_SLOT_FOOTER_DE_THI=

# Shared secret cho POST /api/revalidate webhook (CMS examsAfterChange ping).
# Match cùng giá trị với REVALIDATE_SECRET ở istudy-cms .env.
REVALIDATE_SECRET=changeme
